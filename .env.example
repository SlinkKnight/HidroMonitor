# Copy this file to .env and fill in your Supabase project's values
# (supabase.com/dashboard → Settings → API).

# Client-side (safe to expose — used by the browser Supabase client)
VITE_SUPABASE_URL=https://yqdpgohedxwzkghvjeca.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# Server-side (same project — read by server functions/routes and the MCP endpoint)
SUPABASE_URL=https://yqdpgohedxwzkghvjeca.supabase.co
SUPABASE_PUBLISHABLE_KEY=

# Server-side only — NEVER expose to the client. Only needed if you use
# src/integrations/supabase/client.server.ts (admin operations bypassing RLS).
SUPABASE_SERVICE_ROLE_KEY=
